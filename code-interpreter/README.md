Code Interpretes is only on agentcore and you can think of it as a tool that agent can use
to run code in aws given sandbox environment

```bash
python3.12 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
deactivate
```

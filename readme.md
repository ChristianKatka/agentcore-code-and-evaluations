**Bedrock Evaluations** = benchmark models, model scoring
**AgentCore Evaluations** = agentin evaluaatio, ex. helpfulness, goal success
**code-interpreter** = run code in sandbox. think of it as a tool that agent can use to run code

us-east-1 on käytössä oleva regioona

export AWS_DEFAULT_PROFILE=christianstudent
